import CodeBlock from "@theme/CodeBlock";

import For from "!!raw-loader!./05.for.zig";

# For 循环

For 循环用于迭代数组（以及其他类型，稍微讨论）。For 循环遵循此语法。像 while 一样，for 循环可以使用 `break` 和
`continue`。在这里，我们必须赋值给 `_`，因为 Zig 不允许我们有未被使用的值。

<CodeBlock language="zig">{For}</CodeBlock>
